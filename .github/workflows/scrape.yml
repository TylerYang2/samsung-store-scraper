name: Store Scraper

on:
  workflow_dispatch:
    inputs:
      target:
        description: '실행할 스크래퍼'
        required: true
        default: 'carriers'
        type: choice
        options:
          - carriers
          - all
          - samsung
          - kt
          - lgu
          - skt

concurrency:
  group: store-scraper
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - uses: actions/setup-python@v5
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Run Samsung scraper
        if: inputs.target == 'all' || inputs.target == 'samsung'
        env:
          KAKAO_API_KEY: ${{ secrets.KAKAO_API_KEY }}
        run: python samsung_stores.py

      - name: Run KT scraper
        if: inputs.target == 'carriers' || inputs.target == 'all' || inputs.target == 'kt'
        env:
          BOX_CLIENT_ID:     ${{ secrets.BOX_CLIENT_ID }}
          BOX_CLIENT_SECRET: ${{ secrets.BOX_CLIENT_SECRET }}
          BOX_REFRESH_TOKEN: ${{ secrets.BOX_REFRESH_TOKEN }}
          BOX_ACCESS_TOKEN:  ${{ secrets.BOX_ACCESS_TOKEN }}
        run: python kt_stores.py

      - name: Run LGU+ scraper
        if: inputs.target == 'carriers' || inputs.target == 'all' || inputs.target == 'lgu'
        env:
          BOX_CLIENT_ID:     ${{ secrets.BOX_CLIENT_ID }}
          BOX_CLIENT_SECRET: ${{ secrets.BOX_CLIENT_SECRET }}
          BOX_REFRESH_TOKEN: ${{ secrets.BOX_REFRESH_TOKEN }}
          BOX_ACCESS_TOKEN:  ${{ secrets.BOX_ACCESS_TOKEN }}
        run: python lgu_stores.py

      - name: Run SKT scraper
        if: inputs.target == 'carriers' || inputs.target == 'all' || inputs.target == 'skt'
        env:
          BOX_CLIENT_ID:       ${{ secrets.BOX_CLIENT_ID }}
          BOX_CLIENT_SECRET:   ${{ secrets.BOX_CLIENT_SECRET }}
          BOX_REFRESH_TOKEN:   ${{ secrets.BOX_REFRESH_TOKEN }}
          BOX_ACCESS_TOKEN:    ${{ secrets.BOX_ACCESS_TOKEN }}
          PYTHONUNBUFFERED: '1'
        run: python skt_stores.py

      - name: Upload results
        uses: actions/upload-artifact@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          name: Store_Data
          path: output/
          retention-days: 30
